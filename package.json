{
  "name": "vue-moviepass-groepsboeker",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test",
    "prepare:android": "node scripts/prepare-android-assets.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
